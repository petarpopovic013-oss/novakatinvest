export interface PropertyFact {
  label: string;
  value: string;
}

export interface GalleryItem {
  src: string;
  alt: string;
  label: string;
  number: string;
}

export interface StoryCardData {
  src: string;
  eyebrow: string;
  title: string;
  description: string;
}
